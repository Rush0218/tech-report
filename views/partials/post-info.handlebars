<article class="post">
    <div class="title">
        <h1>{{post_title}}</h1>
        <p>{{post_body}}</p>
    </div>
    <div class="meta">
        <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
    </div>
</article>